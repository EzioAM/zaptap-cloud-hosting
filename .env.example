# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: Service Role Key for admin operations
# Required for running migration scripts and creating test data
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Optional: AI Research Service (if using AI features)
ANTHROPIC_API_KEY=your_anthropic_api_key

# Optional: Weather API Configuration
# Get a free API key from https://openweathermap.org/api
# Without this key, the app will use simulated weather data
OPENWEATHER_API_KEY=your_openweather_api_key

# Optional: Other API Keys
# Add any other API keys your app uses here